# Real-Estate-Python-Web-Application

## heroku
`heroku login`
#### to display the repository you are connected to
`git remote -v`
#### add new heroku repository
`heroku git:remote -a APP_NAME`
#### deploy the app from CLI
`git push heroku main`